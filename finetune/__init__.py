# -*- coding: utf-8 -*-
"""finetune/ — sd-scripts-style tagging / captioning entry points.

Holds the Anima Tagger CLI package (moved from scripts/anima_tagger/). The
captioning *logic* stays in library.captioning (a core primitive); these are the
runnable wrappers around it.
"""
