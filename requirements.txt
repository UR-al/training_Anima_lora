# Anima LoRA — pip dependency manifest.
#
# Mirrors `pyproject.toml` [project.dependencies]. `uv` (install_uv.bat) stays the
# validated path via pyproject.toml + uv.lock; this file is the pip-friendly view
# used by install_pip.bat. Keep the two in sync when adding/removing a library.
#
# Install:  pip install -r requirements.txt   (then `pip install -e . --no-deps`
#           to add the editable anima_lora / library / networks packages.)
#
# torch / torchvision (and the flash-attn wheels) resolve from the CUDA 13.2
# index; pre-releases are allowed (torch 2.12 is a pre-release at pin time).
--extra-index-url https://download.pytorch.org/whl/cu132
--pre

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.12.0,<2.13
torchvision>=0.27.0,<0.28
accelerate>=1.13.0
transformers>=5.10.1
diffusers>=0.37.0
einops>=0.7.0

# ── Optimizers ───────────────────────────────────────────────────────────────
pytorch-optimizer>=3.9.0
prodigyopt>=1.1.2

# ── Adapters (LyCORIS: LoKr/LoHa/DyLoRA/GLoRA/Full/Diag-OFT/BOFT/IA3) ─────────
lycoris-lora>=3.4.0

# ── Data & config ────────────────────────────────────────────────────────────
toml>=0.10.2
tomlkit>=0.13
voluptuous>=0.15.2
safetensors>=0.7.0
imagesize>=1.4.1

# ── Text processing ──────────────────────────────────────────────────────────
sentencepiece>=0.2.1
huggingface-hub>=1.9.0

# ── Monitoring & utilities ───────────────────────────────────────────────────
tensorboard
rich>=14.0.0
tqdm
numpy>=2.0.0
Pillow
psutil
opencv-python>=4.11.0.86

# ── Masking (SAM3 + MIT) ─────────────────────────────────────────────────────
pyyaml
sam3 @ git+https://github.com/facebookresearch/sam3.git
segmentation-models-pytorch>=0.3.4
albumentations>=1.4.0
packaging
pycocotools>=2.0.11

# ── flash-attn (prebuilt cu132 / torch2.12 / cp313 wheels) + Triton ──────────
flash-attn @ https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.17/flash_attn-2.8.3+cu132torch2.12-cp313-cp313-linux_x86_64.whl ; sys_platform == 'linux'
flash-attn @ https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.25/flash_attn-2.8.3+cu132torch2.12-cp313-cp313-win_amd64.whl ; sys_platform == 'win32'
triton-windows>=3.7.0,<3.8.0 ; sys_platform == 'win32'

# ── GUI / lint / misc ────────────────────────────────────────────────────────
pyside6>=6.11.0
# Gradio control panel (`python tasks.py gradio-gui`) — optional in pyproject
# ([project.optional-dependencies].gradio); listed here so the pip "full install"
# view matches. Same major as the kohya_ss GUI this layout is modelled on.
gradio>=5.34.1
# Training-monitor MCP server (tools/anima_monitor_mcp.py) — optional in pyproject
# ([project.optional-dependencies].mcp); listed here so the pip "full install" view matches.
mcp>=1.2.0
ruff>=0.15.9
matplotlib
torchsde>=0.2.6
uv>=0.11.7
yarl>=1.23.0
spandrel>=0.4.2
sqlalchemy>=2.1.0b2
aiohttp>=3.13.5
comfy-cli>=1.10.3
comfy-aimdo>=0.3.0
av>=17.0.1
alembic>=1.18.4
pydantic-settings>=2.14.0
kornia>=0.8.2
comfy-kitchen>=0.2.10
simpleeval>=1.0.7
pytest>=9.0.3
blake3>=1.0.8
softtorch>=0.1.1
controlnet-aux>=0.0.10
scikit-learn>=1.9.0
datasets>=4.8.5
